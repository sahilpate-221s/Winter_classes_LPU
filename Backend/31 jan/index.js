import { sum } from './second.js';
import { multipy } from './second.js';

// Create an array of 5 integer values
const integerValues = [10, 20, 30, 40, 50];

// Call the sum function with the array and log the result
const totalSum = sum(integerValues);
console.log("Total Sum:", totalSum);

multipy(10, 12);
